const FloatingEnroll = () => (
  <a
    href="#admission"
    className="fixed bottom-6 right-6 z-50 md:hidden rounded-full bg-accent text-accent-foreground px-6 py-3 font-semibold text-sm shadow-xl hover:opacity-90 transition-opacity"
  >
    Enroll Now
  </a>
);

export default FloatingEnroll;
